export const metadata = {
  title: "Admin Portal | Travnox",
  description: "Administration portal for managing agencies and travellers",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
